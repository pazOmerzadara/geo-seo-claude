# Create PR Skill
1. Ask user which branches to target (default: both dev and main)
2. Ensure all changes are committed and pushed
3. Create PR to dev first, then create separate PR to main
4. Use conventional commit style for PR title
5. Include summary of changes in PR description
6. Verify no merge conflicts before creating
