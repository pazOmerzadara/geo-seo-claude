# Create supabase-migrations Skill
I need a database migration. Before writing any SQL: 1) Use Task to spawn an agent that runs 'SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = '<TABLE>' ORDER BY ordinal_position' via mcp__supabase__execute_sql to get the ACTUAL current schema. 2) In parallel, spawn another agent to review any existing plan.md or migration files for staleness. 3) Only after confirming the real schema, write the migration SQL. 4) Before applying, show me a diff of schema before/after. 5) Always confirm which Supabase project (dev vs production) I want to target — never assume. 6) After applying, verify the migration succeeded by re-querying the schema.
